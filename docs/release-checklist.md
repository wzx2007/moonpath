# Release Checklist

Current library status: `0.30.0` candidate.

## Required Local Checks

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\verify-moonpath.ps1
cd .\moonpath
moon info
```

Expected verification shape:

- `moon fmt --check` passes.
- `moon test` reports 40 passing tests.
- `moon build` passes.
- `moon run cmd/main` prints the deterministic demo summary.
- `moon run cmd/bench` prints grid, sparse DAG, dense graph, and JSON timing summaries.

## Repository Metadata

- Name: `moonpath`.
- Description: MoonBit graph algorithms and grid pathfinding library.
- License: Apache-2.0.
- Suggested topics: `moonbit`, `graph`, `pathfinding`, `astar`, `dijkstra`, `bellman-ford`, `grid`, `benchmark`.
- GitHub: `https://github.com/wzx2007/moonpath`.
- GitLink: `https://www.gitlink.org.cn/w1tness/moonpath`.

## Before Publishing

- Confirm `README.md`, `README.mbt.md`, `docs/api.md`, and `docs/error-semantics.md` match the public API generated by `moon info`.
- Keep `CHANGELOG.md` top entry aligned with the version being announced.
- Confirm GitHub Actions passes on the pushed commit.
- Re-run the GitLink upload script after every local release commit.
